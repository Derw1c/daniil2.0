def bank (а, годы):

 для i в диапазоне (1, годы + 1):

 a + = a * 0,1

 возврат (a)

a, b = ввод (),input()

x, y = a.isdigit(), b.isdigit()

если x == True и y == True: распечатать(банк(int(a), int(b)))

остальное: печать ("Не цифры")
